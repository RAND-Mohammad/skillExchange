# Submission Checklist

1. Open SQL Server Management Studio and take a screenshot of the five tables: `users`, `categories`, `skills`, `exchangeRequests`, and `feedbacks`.
2. Back up database `rand13231`: right-click the database, select **Tasks > Back Up**, use **Full** backup, and save the `.bak` file.
3. Restore the `.bak` into a test database and take a screenshot of the restored database.
4. Open `ERD.md` in a Mermaid-compatible viewer and take a screenshot of the ERD.
5. In Visual Studio select **Build > Publish**, choose Folder or IIS, create the deployment package, and take a screenshot of the publish result.
6. Test Register, Login, Skills CRUD, Admin report export, request acceptance, and email notification before recording the demo.
